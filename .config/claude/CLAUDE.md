# 共有ルール（dotfiles 管理）

全マシン共通の指示。machine ローカルの `~/.claude/CLAUDE.md` から `@import` で取り込む。

## 日本語文章の作成

PR / Issue / artifacts / openspec の作成時、および markdown ファイルで QA や手順を説明するときは、指示がなくても readable-writing-japanese skill を必ず読み込む。
